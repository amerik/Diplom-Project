= render partial: 'layouts/admin/header'
= render partial: 'layouts/admin/left_menu'

.content_wrapper
  = render partial: 'layouts/admin/left_detail_menu' 
  .data_content
    %ul.paths
      %li= link_to t("view.admin.common.countries"), admin_countries_path
      %li= link_to t("view.admin.country.new_country"), new_admin_country_path
    .h3.marginbottom-20= t("view.admin.country.new_country")

    = form_for Country.new, :url => { :controller => @entity_table, :action => 'create' }, :method => :post do |f|

      = render partial: "admin/#{@entity_table}/form_data", :locals => { :f => f, :entity => Country.new }

      .clearfix
      .margintop-30
        .message_errors
          %ul
        .loading_button
          %img{:src => '/assets/admin/loading_button.gif'}
        = link_to 'save', '#', :class => 'default_button dark create_entity'