= render partial: 'layouts/admin/header'
= render partial: 'layouts/admin/left_menu'

.content_wrapper
  = render partial: 'layouts/admin/left_detail_menu' 
  .data_content
    %ul.paths
      %li= link_to 'Meta tags', admin_meta_tags_path
      %li= link_to 'Edit', edit_admin_meta_tag_path(@meta_tag)
    .h3.marginbottom-20= t("view.admin.country.edit_country")

    = form_for @meta_tag, :url => { :controller => @entity_table, :action => 'update', :id => @meta_tag.id }, :method => :put do |f|

      = render partial: "admin/#{@entity_table}/form_data", :locals => { :f => f, :entity => @meta_tag }

      .clearfix
      .margintop-30
        .message_errors
          %ul
        .loading_button
          %img{:src => '/assets/admin/loading_button.gif'}
        = link_to 'save', '#', :class => 'default_button dark update_entity'